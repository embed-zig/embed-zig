//! Optional `pkg/*` integrations (LVGL, PortAudio, …) now live in split packages
//! under `embed-zig-pkgs/`. This struct remains as the extension point for any
//! future in-tree optional modules.

pub const Packages = struct {};
