const time = @import("time.zig");
const log = @import("log.zig");
const rng = @import("rng.zig");
const system = @import("system.zig");
const thread = @import("thread.zig");
const sync = @import("sync.zig");
const io = @import("io.zig");
const socket = @import("socket.zig");
const netif = @import("netif.zig");
const fs_mod = @import("fs.zig");
const ota_mod = @import("ota_backend.zig");
const time_contract = @import("runtime").time;
const log_contract = @import("runtime").log;
const rng_contract = @import("runtime").rng;
const system_contract = @import("runtime").system;
const thread_contract = @import("runtime").thread;
const sync_contract = @import("runtime").sync;
const io_contract = @import("runtime").io;
const socket_contract = @import("runtime").socket;
const netif_contract = @import("runtime").netif;
const fs_contract = @import("runtime").fs;
const ota_contract = @import("runtime").ota_backend;
const crypto_contract = @import("runtime").crypto;
pub const Time = time.Time;
pub const Log = log.Log;
pub const Rng = rng.Rng;
pub const System = system.System;
pub const Thread = thread.Thread;
pub const Mutex = sync.Mutex;
pub const Condition = sync.Condition;
pub const Notify = sync.Notify;
pub const IO = io.IO;
pub const Socket = socket.Socket;
pub const NetIf = netif.NetIf;
pub const Fs = fs_mod.Fs;
pub const OtaBackend = ota_mod.OtaBackend;
pub const crypto = @import("crypto/root.zig");
pub const Crypto = crypto.Crypto;
comptime {
    _ = time_contract.from(Time);
    _ = log_contract.from(Log);
    _ = rng_contract.from(Rng);
    _ = system_contract.from(System);
    _ = thread_contract.from(Thread);
    _ = sync_contract.Mutex(Mutex);
    _ = sync_contract.ConditionWithMutex(Condition, Mutex);
    _ = sync_contract.Notify(Notify);
    _ = io_contract.from(IO);
    _ = socket_contract.from(Socket);
    _ = netif_contract.from(NetIf);
    _ = fs_contract.from(*Fs);
    _ = ota_contract.from(OtaBackend);
    _ = crypto_contract.suite.from(Crypto);
}
